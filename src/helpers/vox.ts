export class VoxHelper {
    static async load(
        path: string
    ): Promise<[Int32Array, number, number, number]> {
        // TODO: implement
        return [null, -1, -1, -1];
    }
}
